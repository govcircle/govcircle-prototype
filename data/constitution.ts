
import { Constitution } from '../types';
// Empty to satisfy "remove data" but exported to prevent import errors if any legacy code remains
export const constitutionData: Constitution | null = null;
